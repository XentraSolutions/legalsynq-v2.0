You are implementing the following Xenia closure ticket.

==================================================
TICKET
==================================================

Ticket ID: XENIA-P1-T3-V1

Parent Ticket: XENIA-P1 — Xenia Platform Foundation & Email Automation

Related Tickets:
- XENIA-P1-T1 — Core Service Foundation
- XENIA-P1-T1-V1 — Core Foundation Validation and Remediation
- XENIA-P1-T1-V2 — Core Portability and Runtime Closure
- XENIA-P1-T2 — Tenant Email Module Foundation and Source Connectivity
- XENIA-P1-T2-V1 — Email Source Connectivity and Administration Closure
- XENIA-P1-T3 — Email Ingestion Engine and Message Persistence Foundation

Task Type: 🟦 XenIA

Title: Ingestion Runtime Validation and Provider Completion

==================================================
MANDATORY EXECUTION RULES
==================================================

1. Create the implementation report before making any code changes.

Required report path:

/analysis/XENIA-P1-T3-V1-report.md

2. The report must be created as the first implementation action.

3. Populate the initial repository analysis, prerequisite review, toolchain, and implementation-progress sections before modifying source code.

4. Update the report incrementally throughout:

- Repository analysis
- Prior report verification
- Build and publish validation
- Migration validation
- Provider classification
- Controlled protocol environment setup
- Real ingestion validation
- Durable locking implementation
- Cursor-protection implementation
- Retry and resume validation
- Attachment dispatch
- Tenant-isolation validation
- Runtime API validation
- Proxy validation
- HTML and header security
- Audit validation
- Frontend validation
- Final acceptance review

5. Do not create or fill the report only after implementation is complete.

6. Do not fabricate:

- Build success
- Publish success
- Test success
- Migration success
- Provider ingestion
- Authentication
- Cursor continuation
- Duplicate prevention
- Attachment dispatch
- Durable locking
- Tenant isolation
- API validation
- Proxy validation
- HTML sanitization
- Audit delivery
- Frontend validation

7. Clearly classify every activity as one of:

- Completed
- Partially completed
- Failed
- Blocked
- Skipped
- Not applicable
- Not independently verified

8. For every command, test run, migration, HTTP request, protocol operation, and controlled ingestion run, record:

- Exact command or request
- Working directory
- Environment
- Exit code or HTTP status
- Expected result
- Actual result
- Relevant output summary
- Remediation performed
- Retest result

9. Do not perform GitHub operations.

Do not:

- Run git add
- Commit
- Push
- Create pull requests
- Merge branches
- Claim GitHub Actions success

10. Do not perform AWS or production infrastructure work.

Do not:

- Deploy to ECS or Fargate
- Modify production RDS
- Modify production Secrets Manager
- Modify IAM
- Modify Cloud Map
- Modify Route53
- Modify ALB
- Claim production deployment success
- Claim production runtime success

11. Do not begin XENIA-P1-T4.

12. Do not implement:

- AI
- Case creation
- Support-ticket creation
- Workflow routing
- Email sending
- Email reply
- Auto-response
- Message deletion
- Message movement
- Mark-as-read functionality
- OCR
- Document classification
- Calendar synchronization
- Contact synchronization

13. Preserve the locked Xenia architecture.

14. Keep remediation incremental.

15. Do not rewrite unrelated services.

16. Do not use production mailboxes.

17. Do not use production databases.

18. Do not weaken tenant isolation, authorization, TLS, SSRF, sanitization, or secret protections to make validation pass.

==================================================
OBJECTIVE
==================================================

Complete and validate the Xenia Email Ingestion Engine.

This ticket must convert the current implementation from:

Architecturally implemented but incompletely validated

to:

Build-validated, migration-validated, runtime-tested, provider-classified, protocol-proven, tenant-isolated, multi-instance-safe, and security-hardened.

This ticket must:

- Execute and document the full backend build, publish, and test suite.
- Validate the ingestion schema against disposable MySQL.
- Classify all five provider connectors honestly.
- Demonstrate at least one real controlled protocol ingestion path.
- Prove initial and incremental synchronization.
- Prove provider cursor persistence and safe advancement.
- Prove duplicate prevention.
- Replace production reliance on the in-process source lock with durable locking.
- Protect sensitive provider cursor values.
- Validate retry, resume, interruption, and rate-limit behavior.
- Validate streaming attachment dispatch.
- Execute runtime API tests.
- Execute Control Center proxy tests.
- Prove relational tenant isolation.
- Harden message HTML and header rendering.
- Validate audit redaction.
- Formally decide whether XENIA-P1-T3 may close.
- Formally decide whether XENIA-P1-T4 may begin.

==================================================
SOURCE MATERIAL
==================================================

Review these reports before modifying code:

/analysis/XENIA-P1-T1-report.md
/analysis/XENIA-P1-T1-V1-report.md
/analysis/XENIA-P1-T1-V2-report.md
/analysis/XENIA-P1-T2-report.md
/analysis/XENIA-P1-T2-V1-report.md
/analysis/XENIA-P1-T3-report.md

Treat prior reports as claims requiring verification.

Confirm against the repository:

- Files exist.
- Projects compile.
- Tests exist.
- Provider connectors match their reported status.
- MailKit is or is not actually referenced.
- M365 and Google connectors are operational, protocol-complete, validation-only, or stubs.
- IMAP and POP3 ingestion methods are real or mocked.
- Model snapshot contains the ingestion schema.
- Migrations are valid.
- APIs are mapped.
- Frontend routes exist.
- Sanitization is implemented.
- Tenant filters exist.
- Locking is production-safe or development-only.
- Audit events exist.
- Cursor values are protected or unprotected.

Record all discrepancies.

==================================================
LOCKED ARCHITECTURE
==================================================

1. Xenia is an independent, separately deployable automation platform.

2. Email is a Xenia module.

3. LegalSynq is the first host platform and not the owner of Xenia core.

4. Xenia remains tenant-aware and provider-neutral.

5. Provider protocol code belongs only inside provider connectors or provider-specific mapping components.

6. The ingestion orchestrator must remain provider-neutral.

7. Xenia must not directly access another service's database.

8. Credentials and provider tokens remain outside the Xenia database except for explicitly protected cursor state where required.

9. Xenia owns:

- Normalized message records
- Message recipients
- Attachment-reference metadata
- Provider synchronization state
- Ingestion runs
- Retry and resume state
- Duplicate detection
- Source locking abstraction
- Message and sync APIs

10. Documents owns attachment binary storage.

11. Audit owns durable audit persistence.

12. Secret provider owns mailbox credentials and OAuth tokens.

13. Xenia must not store attachment binaries.

14. Xenia must not store plaintext credentials or tokens.

15. Xenia must not include LegalSynq product-domain logic.

16. Control Center is LegalSynq host integration, not the portable Xenia UI core.

==================================================
INITIAL ANALYSIS
==================================================

Before code changes:

1. Inspect:

- Xenia.Domain
- Xenia.Application
- Xenia.Infrastructure
- Xenia.Api
- Xenia.Tests
- Xenia ingestion entities
- Provider connectors
- Connector registry
- Normalizer
- Duplicate detector
- Persistence service
- Sync-state service
- Ingestion orchestrator
- Background worker
- Source lock implementation
- Cursor storage
- Attachment dispatcher
- Documents adapter
- Audit adapter
- APIs
- EF configurations
- Migrations
- XeniaDbContextModelSnapshot
- Control Center message and sync pages
- Xenia API client
- Xenia proxy
- HTML rendering code
- Existing sanitization dependencies
- Project and package manifests

2. Run and record:

dotnet --info
dotnet --list-sdks
dotnet tool list
node --version
pnpm --version
mysql --version
docker --version
docker compose version

3. Confirm:

- Correct .NET SDK
- EF CLI availability
- MySQL availability
- Docker availability
- MailKit package status
- HTML sanitizer availability
- Controlled IMAP or POP3 server strategy
- Test document adapter strategy
- Build and frontend commands
- Current test count

4. Create the required report first.

5. Record the initial state before remediation.

==================================================
IMPLEMENTATION TASKS
==================================================

--------------------------------------------------
1. VERIFY AND FINALIZE BUILD STATE
--------------------------------------------------

Run:

dotnet restore
dotnet build
dotnet publish
dotnet test

Validate individually:

- Xenia.Domain
- Xenia.Application
- Xenia.Infrastructure
- Xenia.Api
- Xenia.Tests

Resolve:

- Missing package references
- MailKit inconsistencies
- OpenAPI package issues
- Project-reference issues
- Nullable runtime defects
- Dependency injection failures
- Publish-time missing files
- Runtime dependency issues
- Test compilation issues

Do not broadly upgrade unrelated projects.

Required result:

- Xenia restore succeeds.
- Xenia build succeeds.
- Xenia publish succeeds.
- Test project compiles.
- All critical tests execute.

Record exact commands and exit codes.

--------------------------------------------------
2. CLASSIFY EVERY PROVIDER CONNECTOR
--------------------------------------------------

Classify:

- Microsoft365
- GoogleWorkspace
- IMAP
- POP3
- ExchangeIMAP

Use exactly one status per connector:

- Operational
- Protocol-complete, unverified
- Validation-only
- Stub
- Blocked

For each connector record:

- Concrete class
- Package/library
- Authentication mechanism
- Secret-resolution path
- Initial cursor behavior
- Incremental cursor behavior
- Pagination
- Body retrieval
- Recipient retrieval
- Attachment descriptor retrieval
- Attachment stream retrieval
- Rate-limit behavior
- Invalid-cursor behavior
- Actual controlled execution evidence

Do not call a connector complete when it returns empty or mocked pages.

--------------------------------------------------
3. RESOLVE MAILKIT IMPLEMENTATION STATUS
--------------------------------------------------

Determine whether MailKit is:

- Installed
- Referenced
- Used by IMAP
- Used by POP3
- Used by Exchange IMAP

If required, add a compatible package version narrowly.

Validate:

- TLS
- Authentication
- Read-only IMAP folder mode
- UID retrieval
- UIDVALIDITY retrieval
- UIDL retrieval
- MIME parsing
- Attachment stream access
- Cancellation
- Connection disposal

Remove contradictory or dead no-op paths where they cause ambiguity.

Do not remove useful validation-only connectors if they remain distinct and documented.

--------------------------------------------------
4. PROVISION DISPOSABLE MYSQL
--------------------------------------------------

Use one of:

- Docker MySQL
- Local MySQL
- Disposable test MySQL
- Another isolated non-production MySQL instance

Do not use production RDS.

Record:

- MySQL version
- Container/process name
- Port
- Database name
- Temporary user strategy
- Cleanup process

Mask credentials in the report.

--------------------------------------------------
5. VALIDATE CLEAN AND UPGRADE MIGRATIONS
--------------------------------------------------

Run:

dotnet ef migrations list
dotnet ef migrations script
dotnet ef database update

Validate:

A. Clean database:
- Apply all Xenia migrations.

B. Upgrade database:
- Apply through the final T2 schema.
- Apply the T3 ingestion migration.

Inspect:

- xn_email_messages
- xn_email_recipients
- xn_email_attachment_references
- xn_email_sync_state
- xn_email_ingestion_runs
- Any lock table introduced

Validate:

- Migration history
- Snapshot consistency
- Provider-message uniqueness
- Sync-state uniqueness
- Tenant indexes
- Source indexes
- Foreign keys
- Enum mappings
- JSON mappings
- Long-text mappings
- Concurrency fields
- No attachment binary column
- No credential column
- No cross-service foreign key

Do not manually insert migration history without verified schema execution.

--------------------------------------------------
6. IMPLEMENT DURABLE SOURCE LOCKING
--------------------------------------------------

Replace production use of the in-process-only source lock.

Preferred implementation:

Database lease row using MySQL and EF.

Suggested table:

xn_email_source_sync_locks

Suggested fields:

- Id
- TenantId
- EmailSourceId
- LeaseOwnerId
- AcquiredAt
- RenewedAt
- ExpiresAt
- Version
- CreatedAt
- UpdatedAt

Required behavior:

- Unique lock per TenantId + EmailSourceId
- Atomic acquisition
- Lease expiration
- Renewal
- Release
- Stale-lock recovery
- Cancellation handling
- Crash recovery
- Worker identity
- Concurrency protection
- Multi-instance safety

Keep the in-process lock only as:

- Test implementation, or
- Explicit development implementation

Production/default implementation must use durable locking.

Add readiness/status information for lock provider.

Tests:

- Same source conflicts across two simulated service instances
- Different sources run concurrently
- Different tenants run concurrently
- Expired lease is recoverable
- Active lease is not stolen
- Cancellation releases or expires safely
- Process interruption leaves recoverable lock

--------------------------------------------------
7. IMPLEMENT CURSOR PROTECTION
--------------------------------------------------

Classify provider cursor sensitivity.

Protect at minimum:

- Microsoft delta tokens
- Google continuation/page tokens
- Other opaque continuation tokens
- Provider cursor metadata containing sensitive state

Implement a platform-neutral protection abstraction such as:

IProviderCursorProtector

Operations:

- ProtectAsync
- UnprotectAsync
- GetVersion
- Rotate or re-protect support where appropriate

Development implementation may use a framework data-protection mechanism.

Requirements:

- Raw cursor not returned through APIs
- Raw cursor not written to logs
- Raw cursor not written to audit
- Safe summary only
- Protection version stored
- Protection failure prevents cursor advancement
- Reset clears protected state
- Invalid protected value triggers controlled reset/failure
- Tenant/source binding prevents cursor swapping

Add tests:

- Round trip
- Wrong tenant fails
- Wrong source fails
- Tampering fails
- API omits raw value
- Logs omit raw value
- Reset clears value
- Failure does not advance sync

--------------------------------------------------
8. CREATE CONTROLLED PROTOCOL HARNESS
--------------------------------------------------

Set up at least one real controlled protocol server.

Preferred:

- Local TLS-enabled IMAP server

Alternative:

- Local TLS-enabled POP3 server
- Purpose-built protocol harness
- Approved provider sandbox

The environment must support:

- TLS
- Test authentication
- At least two messages
- One HTML message
- One attachment
- Stable message identifiers
- Insertion of an additional message for incremental sync
- Safe teardown

Record:

- Server/library used
- Startup command
- Port
- TLS certificate setup
- Test account
- Message fixtures
- Cleanup

Use test-only secrets.

Do not put secret values in the report.

--------------------------------------------------
9. EXECUTE REAL INITIAL INGESTION
--------------------------------------------------

Through the actual connector and orchestrator:

1. Create tenant.
2. Create Email source.
3. Configure test secret reference.
4. Enable Email module.
5. Acquire durable lock.
6. Connect with TLS.
7. Authenticate.
8. Initialize cursor.
9. Discover messages.
10. Retrieve message metadata.
11. Retrieve message body.
12. Normalize.
13. Detect duplicates.
14. Persist.
15. Dispatch at least one attachment through a test Documents adapter.
16. Commit cursor.
17. Complete ingestion run.
18. Release lock.

Verify:

- No message modification
- No mark-as-read
- No delete
- No send
- No credential logging
- No binary persisted in Xenia tables

Record actual counts and IDs using safe redaction.

--------------------------------------------------
10. EXECUTE REAL INCREMENTAL INGESTION
--------------------------------------------------

After initial sync:

1. Add a new test message to the controlled server.
2. Run synchronization again.
3. Verify only the new message imports.
4. Verify prior messages are not duplicated.
5. Verify cursor advances.
6. Verify ingestion counters.
7. Verify attachment behavior.
8. Run a third time with no changes.
9. Verify zero new imports.

If IMAP:

- Validate UID cursor.
- Validate UIDVALIDITY.
- Test UIDVALIDITY change behavior where harness permits.

If POP3:

- Validate UIDL checkpoint.
- Validate duplicate prevention.

--------------------------------------------------
11. VALIDATE DUPLICATE RACE PROTECTION
--------------------------------------------------

Execute:

- Same source replay
- Concurrent same-message persistence
- Same ProviderMessageId
- Same InternetMessageId
- Missing InternetMessageId
- Hash fallback
- Same InternetMessageId across different sources
- Same message through different providers
- Attachment descriptor replay

Validate database unique constraints prevent duplicates under race.

Record actual results.

--------------------------------------------------
12. VALIDATE RETRY AND RESUME
--------------------------------------------------

Simulate:

- Provider timeout
- Temporary network interruption
- 429 or rate-limit equivalent
- Temporary 5xx for HTTP connectors
- Documents adapter temporary failure
- Process interruption
- Partial page failure
- Invalid cursor

Validate:

- Bounded retry
- Exponential backoff
- Jitter
- Retry-After support
- Maximum attempts
- NextEligibleSyncAt
- Non-retryable classification
- No cursor over-advancement
- Duplicate-safe replay
- Resumed run linkage
- Correct final status

--------------------------------------------------
13. VALIDATE ATTACHMENT STREAMING
--------------------------------------------------

Use a controlled message with an attachment.

Validate:

- Stream retrieved from provider connector
- No full unbounded memory buffering
- Filename sanitized
- MIME metadata validated
- Size limit enforced
- Hash computed
- Test Documents adapter receives stream
- Document reference persisted
- Repeated dispatch is idempotent
- Temporary Documents failure retries safely
- Permanent Documents failure produces CompletedWithErrors or appropriate status
- No binary field exists in Xenia database
- No Base64 persistence

Inspect schema and logs.

--------------------------------------------------
14. HARDEN HTML SANITIZATION
--------------------------------------------------

Use a maintained sanitizer library or a clearly tested equivalent.

Sanitize before UI rendering.

Required removal or neutralization:

- script
- iframe
- form
- object
- embed
- base
- meta refresh
- event-handler attributes
- javascript URLs
- data URLs where unsafe
- active SVG
- unsafe CSS
- remote images by default
- tracking pixels
- external resource loading

Requirements:

- Raw HTML storage policy documented.
- UI renders sanitized HTML only.
- Text fallback available.
- Remote images blocked by default.
- No unsafe `dangerouslySetInnerHTML`.
- CSP compatibility documented.

Add XSS fixtures and tests.

--------------------------------------------------
15. HARDEN HEADER SANITIZATION
--------------------------------------------------

Implement or validate:

- Header allowlist or denylist
- Size limit
- Count limit
- Unicode normalization
- Malformed-header handling
- Sensitive-header redaction

Remove or redact at minimum:

- Authorization
- Proxy-Authorization
- Provider token headers
- Internal secret-bearing headers
- Oversized values

Validate:

- API exposes safe headers only
- UI exposes safe headers only
- Audit contains no headers
- Logs contain no headers

--------------------------------------------------
16. VALIDATE AUDIT EVENTS
--------------------------------------------------

Execute tests for:

- sync queued
- sync started
- sync completed
- sync completed with errors
- sync failed
- sync cancelled
- sync resumed
- sync reset
- message imported
- message updated
- message duplicate
- message failed
- attachment dispatched
- attachment failed
- cursor invalidated

Validate fields:

- Tenant ID
- Source ID
- Run ID
- Message ID where applicable
- Provider
- Correlation ID
- Result
- Counts
- Timestamp

Prove absence of:

- Subject where restricted
- Body
- Headers
- Credentials
- Secret references
- Raw cursor
- Raw provider exception
- Attachment content

Validate degraded audit fallback behavior.

--------------------------------------------------
17. EXECUTE RELATIONAL TENANT-ISOLATION TESTS
--------------------------------------------------

Use Tenant A and Tenant B in disposable MySQL.

Prove:

- Tenant A cannot sync Tenant B source
- Tenant A cannot read Tenant B sync state
- Tenant A cannot read Tenant B ingestion history
- Tenant A cannot list Tenant B messages
- Tenant A cannot read Tenant B message detail
- Tenant A cannot read Tenant B attachment metadata
- Tenant A cannot reset Tenant B cursor
- Tenant A cannot access through body override
- Tenant A cannot access through route override
- Tenant A cannot access through query override
- Tenant A cannot access through untrusted header
- Background worker preserves correct tenant
- Audit events carry correct tenant
- Document upload metadata carries correct tenant
- PlatformAdmin behavior is explicit

Use safe 404 or 403 according to platform convention.

--------------------------------------------------
18. START XENIA LOCALLY
--------------------------------------------------

Start the service against disposable MySQL.

Validate:

- Startup
- Dependency injection
- Migration state
- Durable lock provider
- Background worker disabled or configured safely
- Provider connector registry
- Documents test adapter
- Audit fallback
- Authentication
- Tenant context
- Authorization
- Graceful shutdown

Record startup and shutdown evidence.

--------------------------------------------------
19. EXECUTE API RUNTIME TESTS
--------------------------------------------------

Execute:

POST /email/sources/{id}/sync

GET /email/sources/{id}/sync-state

GET /email/sources/{id}/ingestion-history

GET /email/sources/{id}/ingestion-history/{runId}

POST /email/sources/{id}/reset-sync

GET /email/messages

GET /email/messages/{id}

GET /email/messages/{id}/attachments

Validate actual statuses:

- 200
- 202
- 400 or 422
- 401
- 403
- 404
- 409
- 503

Required scenarios:

- Valid manual sync returns 202
- Existing active sync returns 409
- No authentication returns 401
- Missing permission returns 403
- Unknown source returns 404
- Disabled source returns 400 or 422
- Mandatory dependency unavailable returns 503
- Message list works
- Message detail works
- Attachments metadata works
- Reset works with manage permission
- Reset fails while active
- Cross-tenant requests fail

Record correlation IDs.

--------------------------------------------------
20. EXECUTE CONTROL CENTER PROXY TESTS
--------------------------------------------------

Run the Control Center and call Xenia through the host proxy.

Validate:

- 202 preserved
- 409 preserved
- 401 preserved
- 403 preserved
- 404 preserved
- 503 preserved
- Authorization forwarded
- Correlation ID forwarded
- Query parameters forwarded
- Pagination forwarded
- Content type preserved
- No trusted tenant header can be injected
- No credentials logged
- No message body logged

Record actual request and response evidence.

--------------------------------------------------
21. VALIDATE FRONTEND
--------------------------------------------------

Run:

pnpm --filter control-center type-check
pnpm --filter control-center lint
pnpm --filter control-center test
pnpm --filter control-center build

Validate:

/xenia/email/messages

/xenia/email/messages/{id}

/xenia/email/sources/{id}/sync

Message list:

- Filters
- Pagination
- Stable sorting
- Empty state
- Error state
- Unauthorized
- Forbidden
- Backend unavailable

Message detail:

- Sanitized HTML
- Text fallback
- Remote images blocked
- Safe headers
- Recipients
- Attachment references
- No raw provider payload
- No secret data

Sync page:

- Queue action
- 202 queued state
- Running state
- Completed state
- Completed-with-errors state
- Failed state
- 409 conflict
- Retry
- Reset confirmation
- Safe cursor summary
- Ingestion history

No Xenia-related frontend errors may remain.

Clearly separate unrelated pre-existing host issues.

--------------------------------------------------
22. COMPLETE TEST EXECUTION
--------------------------------------------------

Execute all existing and new tests.

The report must include:

- Total discovered tests
- Total executed
- Passed
- Failed
- Skipped
- Duration
- Exact command
- Exit code

Do not leave placeholders such as:

- To be recorded
- In progress
- Estimated
- Expected

Fix critical failures.

Document noncritical limitations honestly.

--------------------------------------------------
23. COMPLETE ACCEPTANCE MATRIX
--------------------------------------------------

Populate all acceptance criteria.

For every item record:

- Criterion
- Status
- Evidence
- Notes

Use:

- Completed
- Partially completed
- Failed
- Blocked
- Not applicable

Do not mark criteria complete without executed evidence.

==================================================
PROVIDER CLASSIFICATION REQUIREMENTS
==================================================

The final report must contain a table like:

| Provider | Classification | Authentication | Initial Sync | Incremental Cursor | Attachments | Live Evidence |
|---|---|---|---|---|---|---|
| Microsoft365 | ... | ... | ... | ... | ... | ... |
| GoogleWorkspace | ... | ... | ... | ... | ... | ... |
| IMAP | ... | ... | ... | ... | ... | ... |
| POP3 | ... | ... | ... | ... | ... | ... |
| ExchangeIMAP | ... | ... | ... | ... | ... | ... |

At least one provider must be Operational based on controlled end-to-end evidence.

Others may be:

- Protocol-complete, unverified
- Validation-only
- Stub
- Blocked

Be precise.

==================================================
DATABASE REQUIREMENTS
==================================================

Required tables:

- xn_email_messages
- xn_email_recipients
- xn_email_attachment_references
- xn_email_sync_state
- xn_email_ingestion_runs

If database locking is used:

- xn_email_source_sync_locks

Required constraints:

- Tenant/source/provider-message uniqueness
- Sync state unique per TenantId + EmailSourceId
- Lock unique per TenantId + EmailSourceId
- Attachment reference uniqueness
- Tenant-owned foreign keys only
- No cross-service foreign keys
- No attachment binary column
- No credential column

Required indexes:

- TenantId
- EmailSourceId
- ProviderType
- ProviderMessageId
- InternetMessageId
- ReceivedAt
- ImportStatus
- ProcessingState
- HasAttachments
- IngestionRunStatus
- StartedAt
- NextEligibleSyncAt
- LastSuccessfulSyncAt
- Lock ExpiresAt

==================================================
REQUIRED TEST COVERAGE
==================================================

Build and startup:

- Restore
- Build
- Publish
- DI resolution
- Local startup
- Graceful shutdown

Migration:

- Migration list
- SQL script
- Clean apply
- Upgrade apply
- Snapshot consistency
- Schema inspection

Provider:

- One real controlled protocol path
- Initial sync
- Incremental sync
- Duplicate replay
- Attachment retrieval
- TLS
- Authentication
- Timeout
- Connection closure

Locking:

- Same source conflict
- Cross-instance conflict
- Different-source concurrency
- Lease expiry
- Stale recovery
- Cancellation

Cursor:

- Protection round trip
- Wrong tenant
- Wrong source
- Tampering
- Reset
- Failure behavior
- API redaction
- Log redaction

Retry and resume:

- 429
- Retry-After
- 5xx
- Timeout
- Network interruption
- Process interruption
- Partial page failure
- Invalid cursor
- Documents failure

Security:

- Tenant A/B isolation
- Override attacks
- HTML XSS
- Remote images
- Header redaction
- Audit redaction
- Log redaction
- Secret masking
- Cursor redaction

Runtime APIs:

- 202
- 409
- 401
- 403
- 404
- 503

Proxy:

- Status preservation
- Auth forwarding
- Correlation forwarding
- Query forwarding
- Tenant safety

Frontend:

- Message list
- Filters
- Pagination
- Message detail
- Sanitized HTML
- Remote image blocking
- Attachment references
- Sync queue
- Running state
- Conflict state
- Failed state
- Reset
- History
- Unauthorized
- Forbidden
- Backend unavailable

==================================================
ACCEPTANCE CRITERIA
==================================================

The ticket is complete only when:

1. All Xenia projects restore successfully.

2. All Xenia projects build successfully.

3. Xenia API publishes successfully.

4. All critical tests execute.

5. Final test count and result are documented.

6. Each connector has an honest classification.

7. At least one controlled provider/protocol path is Operational.

8. Initial sync succeeds.

9. Incremental sync succeeds.

10. A third no-change sync imports zero messages.

11. Duplicate replay creates no duplicate records.

12. Provider cursor persists.

13. Cursor advances only after durable persistence.

14. Cursor reset works.

15. Resume after interruption works.

16. Clean MySQL migration succeeds.

17. Upgrade MySQL migration succeeds.

18. Snapshot matches the model.

19. Required tables exist.

20. Required constraints exist.

21. No attachment binaries are stored.

22. No credentials are stored.

23. Durable source locking exists.

24. Same source conflicts across simulated instances.

25. Different sources may run concurrently.

26. Expired lock recovery works.

27. Retry is bounded.

28. Retry-After is respected where applicable.

29. Attachment streaming succeeds in a controlled test.

30. Attachment dispatch is idempotent.

31. Documents failure does not create binary fallback.

32. Tenant A cannot sync Tenant B source.

33. Tenant A cannot read Tenant B sync state.

34. Tenant A cannot read Tenant B ingestion history.

35. Tenant A cannot read Tenant B messages.

36. Tenant A cannot read Tenant B attachments.

37. Tenant A cannot reset Tenant B cursor.

38. Tenant override attacks fail.

39. Background worker preserves tenant context.

40. xenia.email.sync is enforced.

41. Unauthenticated sync returns 401.

42. Unauthorized sync returns 403.

43. Valid manual sync returns 202.

44. Concurrent sync returns 409.

45. Mandatory dependency failure returns 503.

46. Message list API works.

47. Message detail API works.

48. Attachment metadata API works.

49. Sync-state API works.

50. Ingestion-history API works.

51. Reset-sync API works.

52. Proxy preserves 202.

53. Proxy preserves 409.

54. Proxy preserves 401.

55. Proxy preserves 403.

56. Proxy preserves 404.

57. Proxy preserves 503.

58. Proxy forwards authorization.

59. Proxy forwards correlation IDs.

60. Message HTML is sanitized.

61. Scripts cannot execute.

62. Event-handler attributes are removed.

63. javascript URLs are blocked.

64. Remote images are blocked by default.

65. Safe text fallback exists.

66. Sensitive headers are redacted.

67. Cursor values are protected where required.

68. Raw cursor is absent from APIs.

69. Raw cursor is absent from logs.

70. Audit events are emitted.

71. Audit events contain no message body.

72. Audit events contain no credentials.

73. Audit events contain no raw cursor.

74. Logs contain no message body.

75. Logs contain no credentials.

76. Message list UI renders.

77. Message detail UI renders.

78. Sync UI renders.

79. Queued and running states render.

80. Conflict state renders.

81. Failed state renders.

82. Reset confirmation works.

83. Message filters work.

84. Pagination works.

85. Attachment references display.

86. Control Center Xenia code type-checks.

87. Control Center lint executes.

88. Control Center tests execute where configured.

89. Control Center production build succeeds, or unrelated blockers are isolated.

90. No AI behavior is implemented.

91. No case creation is implemented.

92. No support-ticket creation is implemented.

93. No workflow execution is implemented.

94. No outbound email is implemented.

95. `/analysis/XENIA-P1-T3-V1-report.md` exists.

96. The report was created before code changes.

97. The report was updated incrementally.

98. Validation commands include exit codes.

99. API tests include actual HTTP statuses.

100. Provider test evidence is included.

101. Acceptance matrix is complete.

102. Final completion percentage is evidence-based.

103. The report states whether XENIA-P1-T3 may close.

104. The report states whether XENIA-P1-T4 may begin.

==================================================
OUT OF SCOPE
==================================================

Do not implement:

- AI classification
- AI summarization
- Entity extraction
- Case creation
- Support-ticket creation
- Workflow routing
- Email sending
- Email replies
- Auto-response
- Message deletion
- Message movement
- Mark-as-read
- Microsoft Graph subscriptions
- Gmail push notifications
- Provider webhooks
- OCR
- Attachment classification
- Document indexing
- Calendar synchronization
- Contact synchronization
- GitHub operations
- GitHub Actions
- AWS deployment
- Production RDS changes
- Production Secrets Manager changes
- Production ECS/Fargate changes
- Production Cloud Map changes
- Production Route53 changes
- Production ALB changes
- Production runtime claims

==================================================
KNOWN RESTRICTIONS
==================================================

- Keep remediation incremental.
- Do not rewrite Xenia core.
- Do not weaken tenant isolation.
- Do not weaken authorization.
- Do not weaken TLS.
- Do not weaken SSRF controls.
- Do not bypass HTML sanitization.
- Do not store credentials.
- Do not store attachment binaries.
- Do not log message bodies.
- Do not log raw headers.
- Do not log raw cursors.
- Do not fabricate provider ingestion.
- Do not use production mailboxes.
- Do not use production databases.
- Do not begin XENIA-P1-T4.

==================================================
REQUIRED REPORT STRUCTURE
==================================================

Create before any code change:

/analysis/XENIA-P1-T3-V1-report.md

Use at least:

# XENIA-P1-T3-V1 Ingestion Runtime Validation and Provider Completion Report

## 1. Executive Summary

## 2. Ticket Information
- Ticket ID
- Parent ticket
- Related tickets
- Task type
- Objective
- Current status

## 3. Prior Report Review
- Confirmed claims
- Incorrect claims
- Unverified claims
- Missing evidence

## 4. Initial Repository Analysis

## 5. Toolchain and Environment

## 6. Implementation Progress
Maintain:
- Not started
- In progress
- Completed
- Partially completed
- Blocked
- Failed

## 7. Files Inspected

## 8. Build and Publish Validation

## 9. Provider Classification

## 10. MailKit and Protocol Implementation

## 11. Disposable MySQL Environment

## 12. Migration Validation
- Migration list
- Script generation
- Clean apply
- Upgrade apply
- Snapshot validation
- Schema inspection

## 13. Durable Source Locking

## 14. Cursor Protection

## 15. Controlled Protocol Harness

## 16. Initial Ingestion Evidence

## 17. Incremental Ingestion Evidence

## 18. Duplicate Prevention

## 19. Retry and Resume

## 20. Attachment Streaming

## 21. HTML Sanitization

## 22. Header Sanitization

## 23. Tenant Isolation

## 24. Authorization

## 25. Audit Validation

## 26. Local Runtime Startup and Shutdown

## 27. API Validation
For every request:
- Method
- URL
- Authentication
- Permissions
- Tenant
- Expected status
- Actual status
- Correlation ID
- Safe response summary
- Result

## 28. Control Center Proxy Validation

## 29. Frontend Validation
- Type-check
- Lint
- Tests
- Build
- Xenia-specific result
- Whole-app result

## 30. Tests Added or Updated

## 31. Test Execution Results
- Total discovered
- Total executed
- Passed
- Failed
- Skipped
- Duration
- Command
- Exit code

## 32. Security Review

## 33. Sensitive-Content Review

## 34. Acceptance Criteria Matrix
For every criterion:
- Criterion
- Status
- Evidence
- Notes

## 35. Issues Found

## 36. Remediation Performed

## 37. Remaining Gaps

## 38. Risks and Architecture Concerns

## 39. Environmental Limitations

## 40. Out-of-Scope Confirmation

## 41. Documentation Updated

## 42. XENIA-P1-T3 Closure Recommendation
Use one:
- Close
- Close with accepted limitations
- Keep open
- Blocked

## 43. XENIA-P1-T4 Readiness
Use one:
- Ready to begin
- Ready with prerequisites
- Not ready
- Blocked

## 44. Final Status
Use one:
- Complete
- Complete with limitations
- Partially complete
- Blocked
- Failed

## 45. Completion Percentage

## 46. Follow-Up Recommendations

==================================================
FINAL RESPONSE REQUIREMENTS
==================================================

When implementation and validation are complete:

1. Fully update the report.

2. Remove or resolve all placeholder text such as:

- In progress
- To be recorded
- Estimated
- Expected
- Will be completed

unless the activity genuinely remains blocked and is explicitly documented.

3. Provide a concise final response containing:

- Ticket ID
- Final status
- Completion percentage
- Build result
- Publish result
- Test result
- Migration result
- Provider classification
- Controlled ingestion result
- Initial sync result
- Incremental sync result
- Duplicate-prevention result
- Durable-lock result
- Cursor-protection result
- Retry/resume result
- Attachment-dispatch result
- Tenant-isolation result
- Authorization result
- Audit result
- API runtime result
- Proxy result
- HTML-security result
- Frontend result
- Sensitive-content review
- Known limitations
- XENIA-P1-T3 closure recommendation
- XENIA-P1-T4 readiness
- Exact report path

4. Do not claim GitHub success.

5. Do not claim AWS success.

6. Do not claim production success.

7. Stop after XENIA-P1-T3-V1.

Do not begin XENIA-P1-T4.