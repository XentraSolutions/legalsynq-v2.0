You are continuing the LegalSynq Commerce + Tenant Billing operational maturity track.

FIRST ACTION:
Create the report file FIRST before performing any additional work:

/analysis/LS-COMMERCE-OPS-01-report.md

Update this report incrementally as each major implementation step is completed.

DO NOT wait until the end to write the report.

====================================================
TICKET ID: LS-COMMERCE-OPS-01
TITLE: Operational Audit, Reconciliation & Enterprise Support Tooling
====================================================

# Objective

Add enterprise operational support tooling for Commerce + Tenant Billing to improve:

- operational audit visibility
- entitlement publish history
- lifecycle transition traceability
- reconciliation diagnostics
- support investigation tooling
- operational reporting/export readiness
- safe operational remediation visibility

This ticket builds on:

- LS-COMMERCE-INT-01
- LS-COMMERCE-INT-02
- LS-COMMERCE-INT-03
- LS-COMMERCE-INT-04

This is an additive operational tooling ticket.

This is NOT:
- a billing rewrite
- a domain merge
- a payment workflow rewrite
- a new monitoring platform
- a Control Center redesign
- a new authorization framework

====================================================
GIT / GITHUB REQUIREMENTS
====================================================

Use the ticket ID in all Git operations.

Required conventions:

Branch:
- ls-commerce-ops-01-operational-audit-and-reconciliation

Commit message format:
- LS-COMMERCE-OPS-01 <summary>

Examples:
- LS-COMMERCE-OPS-01 add entitlement publish audit timeline
- LS-COMMERCE-OPS-01 add reconciliation diagnostics panel
- LS-COMMERCE-OPS-01 add lifecycle history visibility

If creating a PR:
- PR title must include LS-COMMERCE-OPS-01

Do not use generic commit messages.

====================================================
ARCHITECTURE RULES
====================================================

Maintain strict separation between:

Platform / SaaS Billing:
Commerce
Platform → Tenant

Tenant Billing:
Tenant → Customer / Client

These domains must remain isolated.

STRICTLY PROHIBITED:

- database merges
- billing table merges
- shared DbContexts
- direct cross-service EF access
- invoice/payment/customer workflow rewrites
- collapsing Commerce and Tenant Billing APIs
- destructive replay tooling
- bulk destructive operational actions
- exposing internal tokens
- bypassing gateway/BFF conventions
- removing standalone mode
- redesigning Control Center
- introducing a new authorization framework

====================================================
PRIMARY SCOPE
====================================================

Implement additive enterprise operational tooling for:

1. Entitlement publish history visibility
2. Billing profile lifecycle audit visibility
3. Commerce ↔ Billing reconciliation diagnostics
4. Operational investigation panels
5. Operational export/report readiness
6. Safe remediation visibility
7. Operational filtering/search improvements
8. Final enterprise operational hardening
9. Implementation report

====================================================
FILES / AREAS TO INSPECT
====================================================

Inspect existing implementations before modifying anything.

Review:

- LS-COMMERCE-INT-01 through INT-04 implementation areas
- Commerce operational diagnostics endpoints
- Tenant Billing profile lifecycle implementation
- existing audit/event/outbox tables or logs
- MonitoringEntityBootstrap and monitoring registry
- Control Center operational panels
- CommerceAccountPanel
- BillingEntitlementPanel
- BillingProfileActionsPanel
- existing audit logging/event publishing patterns
- existing export/download/reporting utilities if present
- BFF route conventions
- existing operational status models/types

Likely areas include:

- apps/control-center/
- apps/services/commerce/
- apps/services/tenant-billing/
- apps/services/monitoring/
- apps/gateway/Gateway.Api/
- shared/building-blocks/
- shared/contracts/
- operational/audit modules if present

Do not assume paths.
Validate actual repository structure first.

====================================================
IMPLEMENTATION TASKS
====================================================

# 1. Add Entitlement Publish History Visibility

Add operational visibility for historical entitlement publishing activity.

Surface where safely available:

- publish timestamp
- publish outcome
- tenant id
- billing account id
- retry count
- publish source/system
- correlation id if available
- failure reason summary if available

Preferred sources:
- Commerce outbox history
- integration event logs
- existing audit/event records

Requirements:

- visibility only
- no replay execution
- no destructive queue operations
- PlatformAdmin only
- server-side BFF routes only

If historical persistence does not exist:
- expose only latest publish state
- document exact limitation in report

# 2. Add Billing Profile Lifecycle Audit Visibility

Add operational visibility for billing profile lifecycle transitions.

Surface where safely available:

- prior status
- new status
- timestamp
- actor/system
- tenant id
- profile id
- transition reason if available

Requirements:

- visibility only
- no direct DB access from UI
- no mutation of historical records
- safe fallback if no history exists

If lifecycle history persistence does not exist:
- document exact limitation
- do not fabricate history

# 3. Add Commerce ↔ Billing Reconciliation Diagnostics

Add operational reconciliation visibility between:

- Commerce entitlement state
- Tenant Billing applied entitlement state

Surface where safely available:

- expected entitlement state
- applied entitlement state
- mismatch indicator
- last publish timestamp
- last applied snapshot timestamp
- stale snapshot detection
- reconciliation health status

Requirements:

- read-only diagnostics
- no automatic reconciliation execution
- no destructive remediation actions

If reconciliation data is incomplete:
- surface partial diagnostics safely
- document missing pieces

# 4. Add Operational Investigation Panels

Enhance operational support workflows.

Recommended additions:

- tenant billing investigation panel
- commerce publish investigation panel
- latest lifecycle events panel
- latest entitlement state panel
- integration health summary panel

Requirements:

- PlatformAdmin only
- reuse existing operational card/panel patterns
- lazy-load large datasets where appropriate
- preserve safe fallback behavior

# 5. Add Operational Export / Reporting Readiness

Add lightweight export/report readiness.

Possible scope:

- JSON export endpoint
- CSV export helper
- downloadable operational summary
- reconciliation summary export

Requirements:

- read-only
- PlatformAdmin only
- server-side generation only
- no sensitive payment information
- no secrets/tokens

Do not build a full reporting subsystem.

# 6. Add Safe Remediation Visibility

Add visibility for remediation opportunities WITHOUT executing destructive operations.

Examples:

- stale entitlement snapshots
- failed publish attempts
- orphaned billing mappings
- missing tenant profile linkage
- inactive but entitled tenants

Requirements:

- visibility only
- no bulk remediation execution
- no destructive controls
- informational severity/status indicators allowed

# 7. Add Operational Filtering / Search Improvements

Enhance operational usability for larger deployments.

Recommended additions:

- filter by tenant id
- filter by billing status
- filter by reconciliation state
- filter by publish outcome
- filter by stale state
- date/time filtering if practical

Client-side filtering is acceptable for current dataset sizes.

Avoid backend redesigns.

# 8. Add Final Operational Hardening

Ensure safe handling for:

- missing audit history
- missing reconciliation data
- stale records
- missing tenant context
- unavailable services
- unsupported export format
- monitoring unavailable
- unauthorized access
- large dataset fallback handling

All operational failures must remain safe:

- no stack traces
- no token exposure
- no connection strings
- no raw exception dumps
- no sensitive payment data

# 9. Add Tests / Validation

Add tests where practical.

Recommended areas:

- PlatformAdmin authorization
- operational export authorization
- reconciliation status rendering
- audit history rendering
- stale-state rendering
- empty-history fallback rendering
- filtering behavior
- safe error handling

If frontend test infrastructure is unavailable:
- document manual validation steps.

====================================================
FILES / AREAS TO MODIFY
====================================================

Modify only additive operational tooling areas.

Expected possible modification areas:

- Commerce operational panels
- Billing operational panels
- investigation UI components
- reconciliation diagnostics components
- operational export BFF routes
- audit history BFF routes
- operational filter/search helpers
- shared operational status types
- monitoring integration helpers
- tests
- /analysis/LS-COMMERCE-OPS-01-report.md

Do NOT modify:

- invoice/payment/customer workflows
- payment provider integrations
- DbContexts to merge domains
- core auth architecture
- tenant resolution architecture
- standalone runtime behavior
- billing enforcement logic
- destructive operational tooling

====================================================
VALIDATION COMMANDS
====================================================

Run relevant validation commands based on actual repository structure.

Likely commands may include:

# Frontend / Control Center

- npm run build
- npm run lint
- npm test
- npx tsc --noEmit
- pnpm build
- pnpm lint
- pnpm test
- pnpm tsc --noEmit

# Commerce

- dotnet build apps/services/commerce/Commerce.sln

# Tenant Billing

- dotnet build apps/services/tenant-billing/Billing.sln

# Monitoring

- dotnet build apps/services/monitoring/Monitoring.Api/Monitoring.Api.csproj

# Platform

Run any additional discovered validation commands.

If commands fail:

- document exact command
- document exact failure
- document whether issue appears pre-existing or caused by this ticket
- do not fabricate success

If commands are skipped:
- document why

====================================================
ACCEPTANCE CRITERIA
====================================================

LS-COMMERCE-OPS-01 is complete ONLY IF:

- /analysis/LS-COMMERCE-OPS-01-report.md was created first
- report updated incrementally throughout implementation
- entitlement publish history visibility implemented or explicitly deferred with reason
- billing profile lifecycle audit visibility implemented or explicitly deferred with reason
- reconciliation diagnostics implemented or explicitly deferred with reason
- operational investigation panels implemented
- operational export/report readiness implemented or explicitly deferred with reason
- remediation visibility implemented without destructive actions
- operational filtering/search improvements implemented
- all operational tooling uses existing auth/RBAC
- all operational routes use server-side BFF patterns
- no billing domains are merged
- no shared DbContext introduced
- no destructive replay/remediation tooling added
- standalone behavior preserved
- tests/manual validations documented
- risks/deferred items documented
- final operational maturity assessment included

====================================================
REQUIRED REPORT STRUCTURE
====================================================

Update incrementally:

/analysis/LS-COMMERCE-OPS-01-report.md

Required sections:

1. Executive Summary
2. Prior Operational Baseline
3. Entitlement Publish History Visibility
4. Billing Lifecycle Audit Visibility
5. Reconciliation Diagnostics
6. Operational Investigation Panels
7. Operational Export / Reporting Readiness
8. Remediation Visibility
9. Operational Filtering / Search Improvements
10. Operational Safety Hardening
11. RBAC / Authorization Validation
12. Files Changed
13. Build/Test Validation
14. Risks / Deferred Items
15. Confirmation of Non-Merge Boundaries
16. Final Operational Maturity Assessment
17. Recommended Next Step

====================================================
KNOWN RESTRICTIONS
====================================================

- Do not fabricate validation success.
- Do not claim tests passed unless actually run.
- Do not hide failures.
- Do not redesign Control Center.
- Do not redesign monitoring architecture.
- Do not expose secrets or internal tokens.
- Do not directly query billing databases from UI.
- Do not collapse Commerce and Tenant Billing into one service.
- Do not add destructive replay tooling.
- Do not add bulk remediation execution.
- Do not change invoice/payment processing behavior.
- Do not introduce a new auth framework.

====================================================
EXPECTED OUTPUT
====================================================

At completion:

- PlatformAdmin users can investigate entitlement publishing history.
- PlatformAdmin users can inspect lifecycle transition history.
- Commerce ↔ Billing reconciliation state is operationally visible.
- Operational mismatches and stale states are easier to diagnose.
- Operational support maturity is significantly improved.
- Export/report readiness exists for operational investigations.
- The integration remains bounded-context safe and production operationally mature.

====================================================
FINAL INSTRUCTION
====================================================

Preserve bounded contexts.

Integrate through:

- existing gateway/BFF patterns
- existing auth/RBAC
- operational visibility panels
- monitoring integration
- safe diagnostics visibility

Do not merge billing domains.

Do not redesign billing.

Create and maintain the report first.

Do not fabricate success.