import { TenantSelectionScreen } from './index';

describe('TenantSelectionScreen', () => {
  it('exports the screen entrypoint', () => {
    expect(typeof TenantSelectionScreen).toBe('function');
  });
});
