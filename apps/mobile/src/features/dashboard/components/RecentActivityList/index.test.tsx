import { RecentActivityList } from './index';

describe('RecentActivityList', () => {
  it('exports the component entrypoint', () => {
    expect(typeof RecentActivityList).toBe('function');
  });
});
