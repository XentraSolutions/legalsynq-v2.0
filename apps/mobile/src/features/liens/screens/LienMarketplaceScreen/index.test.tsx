import { LienMarketplaceScreen } from './index';

describe('LienMarketplaceScreen', () => {
  it('exports the screen entrypoint', () => {
    expect(typeof LienMarketplaceScreen).toBe('function');
  });
});
